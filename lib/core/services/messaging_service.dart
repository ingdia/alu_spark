/// Messaging is now handled entirely by [MessageRepositoryImpl] backed by
/// [LocalMessageStore]. This class is kept as a stub so existing provider
/// references compile without changes.
class MessagingService {
  const MessagingService();
}
